import { ScrollfollowDirective } from './scrollfollow.directive';

describe('ScrollfollowDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollfollowDirective();
    expect(directive).toBeTruthy();
  });
});
